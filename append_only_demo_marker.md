# Append-only demo marker

This file was added by ChatGPT as a safe append-only repository change.

It does not modify `demo_chatgpt_update.ipynb`, so the notebook's saved outputs remain untouched.

Workflow rule going forward: append new cells/sections only, and do not rebuild or clear existing notebook outputs unless explicitly approved.
